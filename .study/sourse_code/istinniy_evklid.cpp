#include <iostream>
using namespace std;

int evklid (int a,int b)
{
while(b!=0)
{//нам нужно что бы "а" больше "б". дальше делим "а" на "б", то что в остатке нужно будет снова делить 
 //то что в остатке нужно будет снова делить на число "б", то есть допустим 48 и 36
 //делим 48 на 36, выходит остаток 12. дальше остаток 12 делим на "б", на 36
 //36 делится нацело на 12, поэтому больше ничего не делаем
 //в этом суть.
 /*✅ Как правильно сформулировать словами:

    Алгоритм Евклида работает так:

        Делим большее число a на меньшее b, находим остаток r

        Теперь a = b, b = r

        Повторяем шаги, пока остаток не станет 0

        Когда b == 0, значение a — это и есть GCD

💡 Запомни простое правило:

    Следующая пара всегда b и a % b, а не “остаток и b” и не “остаток делим на b”
    ✍️ Что ты написал в комментарии (разбор по пунктам):

    "Нам нужно чтобы a больше b."

🔸 Нет, не обязательно.
gcd(12, 36) работает так же, как gcd(36, 12) — потому что a % b сам всё расставит на свои места.

    "Дальше делим a на b, остаток снова делим на b..."

🔸 ❌ Вот тут у тебя путаница.

🔴 Неправильно: остаток r = a % b делим на b
🔵 Правильно: новая пара становится b и r
    */
cout << "a = " << a << ", b = " << b << ", a % b = " << a % b << endl;
int temp=b;
b= (a % b);
       a=temp;	
}
return a;
}
int main()
{
int a;
int b;
cout<< "enter a:\n>";
cin>>a;
cout<< "enter b:\n>";
cin>>b;
int res= evklid(a,b);
cout<<"NOD is equel "<< res<<'\n';
	return 0;
}
